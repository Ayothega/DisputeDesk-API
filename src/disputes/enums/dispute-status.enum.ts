export enum DisputeStatus {
  OPEN = "OPEN",
  IN_PROGRESS = "IN_PROGRESS",
  ESCALATED = "ESCALATED",
  RESOLVED = "RESOLVED",
  CLOSED = "CLOSED",
}

export enum UserRole {
  AGENT = "AGENT",
  SUPERVISOR = "SUPERVISOR",
  ADMIN = "ADMIN",
}
